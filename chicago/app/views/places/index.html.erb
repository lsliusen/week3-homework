<style>
  h1 {
    border-bottom: solid 3px gray;
    margin-bottom: 24px;
  }
</style>

<a href="/places/new"> Add a new place  </>

<ul>

  <% @places.each do |place| %>
    <li>
    <%= link_to place.title, "/places/#{place.id}" %>
    ($<%= place.admission_price %>)
    (<%= link_to "delete", "/places/#{place.id}/delete"%>)
    </li>
  <% end %>

</ul>
